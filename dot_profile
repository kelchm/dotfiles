# .profile is sourced for login sh/bash shells (zsh and fish ignore it).
# NOTE: when ~/.bash_profile exists, bash reads it INSTEAD of ~/.profile, so our
# .bash_profile sources this explicitly. Keep this to environment only.
[ -f "$HOME/.config/shell/env.sh" ] && . "$HOME/.config/shell/env.sh"

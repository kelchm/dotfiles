# .zshrc is sourced for interactive zsh shells
eval "$(starship init zsh)"
if command -v mise >/dev/null 2>&1; then
  eval "$(mise activate zsh)"
fi
# Environment (PATH, brew, SSH_AUTH_SOCK) is set in ~/.config/shell/env.sh via .zshenv.
